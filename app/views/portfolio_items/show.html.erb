<p>
  <strong>Title:</strong>
  <%= @portfolio_item.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @portfolio_item.description %>
</p>

<div>
	<iframe src='https://www.youtube.com/embed/<%= @portfolio_item.youtube_id %>?rel=0&autoplay=0' frameborder='0' allowfullscreen></iframe>
</div>

<% if user_signed_in? %>
	<%= link_to 'Edit', edit_portfolio_item_path(@portfolio_item) %> |
<% end %>

<%= link_to 'Back', portfolio_items_path %>
